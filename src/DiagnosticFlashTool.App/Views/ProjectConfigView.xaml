<UserControl x:Class="DiagnosticFlashTool.App.Views.ProjectConfigView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:controls="clr-namespace:DiagnosticFlashTool.App.Views.Controls"
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes">
    <Grid Style="{StaticResource PageRootGridStyle}">
        <Grid.RowDefinitions>
            <RowDefinition Style="{StaticResource PageHeaderRowStyle}" />
            <RowDefinition Style="{StaticResource PageHeaderGapRowStyle}" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <controls:PageHeader Grid.Row="0"
                             Title="项目配置"
                             Subtitle="维护项目通信参数、BOOT 配置与固件文件">
            <controls:PageHeader.Actions>
                <controls:PageHelpDocumentButton />
            </controls:PageHeader.Actions>
        </controls:PageHeader>

        <Grid Grid.Row="2">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="Auto" />
            </Grid.ColumnDefinitions>

            <Border Grid.Column="0" MinWidth="500" Style="{StaticResource Card.Compact}">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="12" />
                        <RowDefinition Height="*" />
                    </Grid.RowDefinitions>

                    <Grid Grid.Row="0" Style="{StaticResource CardContentPanelHeaderGridStyle}">
                        <TextBlock Text="项目列表"
                                   HorizontalAlignment="Center"
                                   Style="{StaticResource CardHeaderTextStyle}" />

                        <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                            <Button Command="{Binding RefreshProjectsCommand}"
                                    Style="{StaticResource Button.ToolBar}"
                                    ToolTip="刷新项目列表">
                                <StackPanel Style="{StaticResource Button.InlineContent}">
                                    <materialDesign:PackIcon Kind="{x:Static materialDesign:PackIconKind.Refresh}"
                                                             Style="{StaticResource Button.PackIcon.Current}" />
                                    <TextBlock Text="刷新" Style="{StaticResource Button.IconLabel}" />
                                </StackPanel>
                            </Button>

                            <Button Command="{Binding NewProjectCommand}"
                                    Margin="0"
                                    Style="{StaticResource Button.ToolBar}"
                                    ToolTip="新增项目">
                                <StackPanel Style="{StaticResource Button.InlineContent}">
                                    <materialDesign:PackIcon Kind="{x:Static materialDesign:PackIconKind.Plus}"
                                                             Style="{StaticResource Button.PackIcon.Current}" />
                                    <TextBlock Text="新增项目" Style="{StaticResource Button.IconLabel}" />
                                </StackPanel>
                            </Button>
                        </StackPanel>
                    </Grid>

                    <DataGrid Grid.Row="2"
                              ItemsSource="{Binding Projects}"
                              SelectedItem="{Binding SelectedProject, Mode=TwoWay}"
                              IsReadOnly="True">
                        <DataGrid.Resources>
                            <Style x:Key="ProjectConfigCellTextStyle" TargetType="TextBlock">
                                <Setter Property="HorizontalAlignment" Value="Left" />
                                <Setter Property="Margin" Value="16,0,0,0" />
                                <Setter Property="VerticalAlignment" Value="Center" />
                            </Style>
                        </DataGrid.Resources>
                        <DataGrid.ColumnHeaderStyle>
                            <Style TargetType="DataGridColumnHeader"
                                   BasedOn="{StaticResource {x:Type DataGridColumnHeader}}">
                                <Setter Property="HorizontalContentAlignment" Value="Center" />
                            </Style>
                        </DataGrid.ColumnHeaderStyle>
                        <DataGrid.InputBindings>
                            <MouseBinding MouseAction="LeftDoubleClick" Command="{Binding EditProjectCommand}" />
                        </DataGrid.InputBindings>
                        <DataGrid.Columns>
                            <DataGridTextColumn Header="项目名称" Binding="{Binding ProjectName}" Width="150" ElementStyle="{StaticResource ProjectConfigCellTextStyle}" />
                            <DataGridTextColumn Header="项目码" Binding="{Binding ProjectNo}" Width="80" ElementStyle="{StaticResource ProjectConfigCellTextStyle}" />
                            <DataGridTextColumn Header="波特率" Binding="{Binding BaudRate}" Width="90" ElementStyle="{StaticResource ProjectConfigCellTextStyle}" />
                            <DataGridTextColumn Header="物理请求" Binding="{Binding PhysicalRequestId}" Width="135" ElementStyle="{StaticResource ProjectConfigCellTextStyle}" />
                            <DataGridTextColumn Header="功能请求" Binding="{Binding FunctionalRequestId}" Width="135" ElementStyle="{StaticResource ProjectConfigCellTextStyle}" />
                            <DataGridTextColumn Header="响应地址" Binding="{Binding ResponseAddressId}" Width="135" ElementStyle="{StaticResource ProjectConfigCellTextStyle}" />
                            <DataGridTextColumn Header="BOOT 配置" Binding="{Binding BootConfigFile}" Width="130" ElementStyle="{StaticResource ProjectConfigCellTextStyle}" />
                            <DataGridTextColumn Header="驱动文件" Binding="{Binding DriveFilePath}" Width="180" ElementStyle="{StaticResource ProjectConfigCellTextStyle}" />
                            <DataGridTextColumn Header="应用文件" Binding="{Binding FlashFilePath}" Width="*" ElementStyle="{StaticResource ProjectConfigCellTextStyle}" />
                        </DataGrid.Columns>
                    </DataGrid>
                </Grid>
            </Border>

            <Border Grid.Column="1" Width="400" Margin="16,0,0,0">
                <Border.Style>
                    <Style TargetType="Border" BasedOn="{StaticResource Card.Compact}">
                        <Setter Property="Visibility" Value="Collapsed" />
                        <Style.Triggers>
                            <DataTrigger Binding="{Binding IsProjectEditorVisible}" Value="True">
                                <Setter Property="Visibility" Value="Visible" />
                            </DataTrigger>
                        </Style.Triggers>
                    </Style>
                </Border.Style>
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="12" />
                        <RowDefinition Height="*" />
                        <RowDefinition Height="16" />
                        <RowDefinition Height="Auto" />
                    </Grid.RowDefinitions>

                    <Grid Grid.Row="0">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*" />
                            <ColumnDefinition Width="Auto" />
                        </Grid.ColumnDefinitions>
                        <TextBlock Grid.Column="0"
                                   Text="{Binding ProjectEditorTitle}"
                                   Style="{StaticResource CardHeaderTextStyle}" />
                        <Button Grid.Column="1"
                                Command="{Binding CancelProjectEditorCommand}"
                                Style="{StaticResource Button.Icon}"
                                ToolTip="关闭">
                            <materialDesign:PackIcon Kind="{x:Static materialDesign:PackIconKind.Close}"
                                                     Style="{StaticResource Button.PackIcon.Current}" />
                        </Button>
                    </Grid>

                    <ScrollViewer Grid.Row="2"
                                  HorizontalScrollBarVisibility="Disabled"
                                  VerticalScrollBarVisibility="Auto">
                        <StackPanel>
                            <Grid Margin="0,0,0,12">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="100" />
                                    <ColumnDefinition Width="*" />
                                </Grid.ColumnDefinitions>
                                <TextBlock Grid.Column="0" Text="项目名称" Style="{StaticResource FieldLabelStyle}" />
                                <TextBox Grid.Column="1"
                                         Height="32"
                                         Text="{Binding ProjectEditor.ProjectName, UpdateSourceTrigger=PropertyChanged}"
                                         Style="{StaticResource SoftTextBoxStyle}" />
                            </Grid>

                            <Grid Margin="0,0,0,12">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="100" />
                                    <ColumnDefinition Width="*" />
                                </Grid.ColumnDefinitions>
                                <TextBlock Grid.Column="0" Text="项目码" Style="{StaticResource FieldLabelStyle}" />
                                <TextBox Grid.Column="1"
                                         Height="32"
                                         Text="{Binding ProjectEditor.ProjectNoText, UpdateSourceTrigger=PropertyChanged}"
                                         Style="{StaticResource SoftTextBoxStyle}" />
                            </Grid>

                            <Grid Margin="0,0,0,12">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="100" />
                                    <ColumnDefinition Width="*" />
                                </Grid.ColumnDefinitions>
                                <TextBlock Grid.Column="0" Text="物理请求" Style="{StaticResource FieldLabelStyle}" />
                                <TextBox Grid.Column="1"
                                         Height="32"
                                         Text="{Binding ProjectEditor.PhysicalRequestId, UpdateSourceTrigger=PropertyChanged}"
                                         Style="{StaticResource SoftTextBoxStyle}" />
                            </Grid>

                            <Grid Margin="0,0,0,12">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="100" />
                                    <ColumnDefinition Width="*" />
                                </Grid.ColumnDefinitions>
                                <TextBlock Grid.Column="0" Text="响应地址" Style="{StaticResource FieldLabelStyle}" />
                                <TextBox Grid.Column="1"
                                         Height="32"
                                         Text="{Binding ProjectEditor.ResponseAddressId, UpdateSourceTrigger=PropertyChanged}"
                                         Style="{StaticResource SoftTextBoxStyle}" />
                            </Grid>

                            <Grid Margin="0,0,0,12">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="100" />
                                    <ColumnDefinition Width="*" />
                                </Grid.ColumnDefinitions>
                                <TextBlock Grid.Column="0" Text="功能请求" Style="{StaticResource FieldLabelStyle}" />
                                <TextBox Grid.Column="1"
                                         Height="32"
                                         Text="{Binding ProjectEditor.FunctionalRequestId, UpdateSourceTrigger=PropertyChanged}"
                                         Style="{StaticResource SoftTextBoxStyle}" />
                            </Grid>

                            <Grid Margin="0,0,0,12">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="100" />
                                    <ColumnDefinition Width="*" />
                                </Grid.ColumnDefinitions>
                                <TextBlock Grid.Column="0" Text="BOOT 配置" Style="{StaticResource FieldLabelStyle}" />
                                <ComboBox Grid.Column="1"
                                          Width="Auto"
                                          HorizontalAlignment="Stretch"
                                          ItemsSource="{Binding BootConfigFiles}"
                                          SelectedItem="{Binding ProjectEditor.BootConfigFile, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                                          Style="{StaticResource StandardComboBoxStyle}" />
                            </Grid>

                            <Grid Margin="0,0,0,12">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="100" />
                                    <ColumnDefinition Width="*" />
                                </Grid.ColumnDefinitions>
                                <TextBlock Grid.Column="0" Text="波特率" Style="{StaticResource FieldLabelStyle}" />
                                <ComboBox Grid.Column="1"
                                          Width="Auto"
                                          HorizontalAlignment="Stretch"
                                          ItemsSource="{Binding BaudRates}"
                                          SelectedItem="{Binding ProjectEditor.BaudRate, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                                          Style="{StaticResource StandardComboBoxStyle}" />
                            </Grid>

                            <Grid Margin="0,0,0,12">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="100" />
                                    <ColumnDefinition Width="*" />
                                </Grid.ColumnDefinitions>
                                <TextBlock Grid.Column="0" Text="驱动文件" Style="{StaticResource FieldLabelStyle}" />
                                <Grid Grid.Column="1">
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="*" />
                                        <ColumnDefinition Width="8" />
                                        <ColumnDefinition Width="Auto" />
                                    </Grid.ColumnDefinitions>
                                    <TextBox Grid.Column="0"
                                             Height="32"
                                             Text="{Binding ProjectEditor.DriverFilePath, UpdateSourceTrigger=PropertyChanged}"
                                             Style="{StaticResource SoftTextBoxStyle}" />
                                    <Button Grid.Column="2"
                                            Command="{Binding BrowseProjectEditorDriverCommand}"
                                            Margin="0"
                                            Style="{StaticResource Button.ToolBar}">
                                        <StackPanel Style="{StaticResource Button.InlineContent}">
                                            <materialDesign:PackIcon Kind="{x:Static materialDesign:PackIconKind.FolderOpenOutline}"
                                                                     Style="{StaticResource Button.PackIcon.Current}" />
                                            <TextBlock Text="浏览" Style="{StaticResource Button.IconLabel}" />
                                        </StackPanel>
                                    </Button>
                                </Grid>
                            </Grid>

                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="100" />
                                    <ColumnDefinition Width="*" />
                                </Grid.ColumnDefinitions>
                                <TextBlock Grid.Column="0" Text="应用文件" Style="{StaticResource FieldLabelStyle}" />
                                <Grid Grid.Column="1">
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="*" />
                                        <ColumnDefinition Width="8" />
                                        <ColumnDefinition Width="Auto" />
                                    </Grid.ColumnDefinitions>
                                    <TextBox Grid.Column="0"
                                             Height="32"
                                             Text="{Binding ProjectEditor.ApplicationFilePath, UpdateSourceTrigger=PropertyChanged}"
                                             Style="{StaticResource SoftTextBoxStyle}" />
                                    <Button Grid.Column="2"
                                            Command="{Binding BrowseProjectEditorApplicationCommand}"
                                            Margin="0"
                                            Style="{StaticResource Button.ToolBar}">
                                        <StackPanel Style="{StaticResource Button.InlineContent}">
                                            <materialDesign:PackIcon Kind="{x:Static materialDesign:PackIconKind.FolderOpenOutline}"
                                                                     Style="{StaticResource Button.PackIcon.Current}" />
                                            <TextBlock Text="浏览" Style="{StaticResource Button.IconLabel}" />
                                        </StackPanel>
                                    </Button>
                                </Grid>
                            </Grid>
                        </StackPanel>
                    </ScrollViewer>

                    <StackPanel Grid.Row="4" Orientation="Horizontal" HorizontalAlignment="Right">
                        <Button Content="取消"
                                Command="{Binding CancelProjectEditorCommand}"
                                Style="{StaticResource Button.DialogSecondary}" />
                        <Button Command="{Binding DeleteProjectEditorCommand}">
                            <Button.Style>
                                <Style TargetType="Button" BasedOn="{StaticResource Button.DialogSecondary}">
                                    <Setter Property="Width" Value="88" />
                                    <Setter Property="Visibility" Value="Collapsed" />
                                    <Style.Triggers>
                                        <DataTrigger Binding="{Binding IsEditingProject}" Value="True">
                                            <Setter Property="Visibility" Value="Visible" />
                                        </DataTrigger>
                                    </Style.Triggers>
                                </Style>
                            </Button.Style>
                            <TextBlock Text="删除项目" />
                        </Button>
                        <Button Command="{Binding SaveProjectEditorCommand}"
                                Style="{StaticResource Button.DialogPrimary}">
                            <StackPanel Style="{StaticResource Button.InlineContent}">
                                <materialDesign:PackIcon Kind="{x:Static materialDesign:PackIconKind.ContentSaveOutline}"
                                                         Style="{StaticResource Button.PackIcon.Current}" />
                                <TextBlock Text="保存项目" Style="{StaticResource Button.IconLabel}" />
                            </StackPanel>
                        </Button>
                    </StackPanel>
                </Grid>
            </Border>
        </Grid>
    </Grid>
</UserControl>
