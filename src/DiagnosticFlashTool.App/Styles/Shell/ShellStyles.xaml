<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                    xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes">
    <ResourceDictionary.MergedDictionaries>
        <ResourceDictionary Source="../Brushes.xaml" />
    </ResourceDictionary.MergedDictionaries>

    <Style x:Key="ShellContentTabControlStyle" TargetType="TabControl">
        <Setter Property="BorderThickness" Value="0" />
        <Setter Property="Background" Value="{StaticResource AppBackgroundBrush}" />
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="TabControl">
                    <Border Background="{TemplateBinding Background}">
                        <ContentPresenter ContentSource="SelectedContent" />
                    </Border>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <Style x:Key="ShellNavGroupTextStyle" TargetType="TextBlock">
        <Setter Property="Width" Value="256" />
        <Setter Property="Height" Value="28" />
        <Setter Property="Margin" Value="0,14,0,4" />
        <Setter Property="Padding" Value="22,0,16,0" />
        <Setter Property="FontSize" Value="13" />
        <Setter Property="FontWeight" Value="SemiBold" />
        <Setter Property="Foreground" Value="{StaticResource SidebarMutedTextBrush}" />
        <Setter Property="VerticalAlignment" Value="Center" />
    </Style>

    <Style x:Key="ShellNavRadioButtonStyle" TargetType="RadioButton">
        <Setter Property="Width" Value="256" />
        <Setter Property="Height" Value="42" />
        <Setter Property="Margin" Value="0" />
        <Setter Property="BorderThickness" Value="0" />
        <Setter Property="Background" Value="Transparent" />
        <Setter Property="Foreground" Value="{StaticResource SidebarTextBrush}" />
        <Setter Property="FontSize" Value="13" />
        <Setter Property="FontWeight" Value="SemiBold" />
        <Setter Property="Cursor" Value="Hand" />
        <Setter Property="GroupName" Value="ShellNavigation" />
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="RadioButton">
                    <Border x:Name="NavBorder"
                            Background="{TemplateBinding Background}"
                            SnapsToDevicePixels="True">
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="4" />
                                <ColumnDefinition Width="26" />
                                <ColumnDefinition Width="34" />
                                <ColumnDefinition Width="*" />
                            </Grid.ColumnDefinitions>

                            <Border x:Name="ActiveRail"
                                    Background="Transparent" />

                            <materialDesign:PackIcon x:Name="NavIcon"
                                                     Grid.Column="2"
                                                     Kind="{Binding Tag, RelativeSource={RelativeSource TemplatedParent}}"
                                                     Width="18"
                                                     Height="18"
                                                     Foreground="{Binding Foreground, RelativeSource={RelativeSource TemplatedParent}, FallbackValue=#A9C7EA, TargetNullValue=#A9C7EA}"
                                                     HorizontalAlignment="Center"
                                                     VerticalAlignment="Center" />

                            <TextBlock x:Name="NavText"
                                       Grid.Column="3"
                                       Text="{TemplateBinding Content}"
                                       Foreground="{Binding Foreground, RelativeSource={RelativeSource TemplatedParent}, FallbackValue=#A9C7EA, TargetNullValue=#A9C7EA}"
                                       FontSize="{TemplateBinding FontSize}"
                                       FontWeight="{TemplateBinding FontWeight}"
                                       VerticalAlignment="Center"
                                       TextTrimming="CharacterEllipsis" />
                        </Grid>
                    </Border>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter TargetName="NavBorder" Property="Background" Value="{StaticResource SidebarHoverBrush}" />
                            <Setter TargetName="NavIcon" Property="Foreground" Value="#FFFFFF" />
                            <Setter TargetName="NavText" Property="Foreground" Value="#FFFFFF" />
                        </Trigger>
                        <Trigger Property="IsChecked" Value="True">
                            <Setter TargetName="NavBorder" Property="Background" Value="{StaticResource PrimaryBrush}" />
                            <Setter TargetName="ActiveRail" Property="Background" Value="#FFFFFF" />
                            <Setter TargetName="NavIcon" Property="Foreground" Value="#FFFFFF" />
                            <Setter TargetName="NavText" Property="Foreground" Value="#FFFFFF" />
                        </Trigger>
                        <Trigger Property="IsKeyboardFocusWithin" Value="True">
                            <Setter TargetName="ActiveRail" Property="Background" Value="{StaticResource PrimaryActiveBrush}" />
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>
</ResourceDictionary>
