<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <ResourceDictionary.MergedDictionaries>
        <ResourceDictionary Source="../Brushes.xaml" />
        <ResourceDictionary Source="CardStyles.xaml" />
    </ResourceDictionary.MergedDictionaries>

    <Thickness x:Key="PageMargin">24</Thickness>
    <Thickness x:Key="PageCardBottomGap">0,0,0,12</Thickness>
    <Thickness x:Key="PageCardTopGap">0,12,0,0</Thickness>
    <Thickness x:Key="PageContentBottomGap">0,0,0,12</Thickness>
    <Thickness x:Key="PageContentTopGap">0,12,0,0</Thickness>
    <Thickness x:Key="FormAssistTextAfterLabelMargin">90,0,0,0</Thickness>

    <Style x:Key="PageRootGridStyle" TargetType="Grid">
        <Setter Property="Margin" Value="{StaticResource PageMargin}" />
    </Style>

    <Style x:Key="PageHeaderRowStyle" TargetType="RowDefinition">
        <Setter Property="Height" Value="88" />
    </Style>

    <Style x:Key="PageHeaderGapRowStyle" TargetType="RowDefinition">
        <Setter Property="Height" Value="24" />
    </Style>

    <Style x:Key="PageSectionGapRowStyle" TargetType="RowDefinition">
        <Setter Property="Height" Value="12" />
    </Style>

    <Style x:Key="PageSummaryRowStyle" TargetType="RowDefinition">
        <Setter Property="Height" Value="122" />
    </Style>

    <Style x:Key="PageColumnGapStyle" TargetType="ColumnDefinition">
        <Setter Property="Width" Value="12" />
    </Style>

    <Style x:Key="FormLabelColumnStyle" TargetType="ColumnDefinition">
        <Setter Property="Width" Value="90" />
    </Style>

    <Style x:Key="PageHeaderCardStyle" TargetType="Border" BasedOn="{StaticResource Card.Elevated}">
        <Setter Property="Padding" Value="{StaticResource CardHeaderContentPadding}" />
    </Style>

    <Style x:Key="PageLeadStandardCardStyle" TargetType="Border" BasedOn="{StaticResource Card.Standard}">
        <Setter Property="Margin" Value="{StaticResource PageCardBottomGap}" />
    </Style>

    <Style x:Key="PageTrailingCompactCardStyle" TargetType="Border" BasedOn="{StaticResource Card.Compact}">
        <Setter Property="Margin" Value="{StaticResource PageCardTopGap}" />
    </Style>

    <Style x:Key="PageTitleTextStyle" TargetType="TextBlock">
        <Setter Property="FontSize" Value="22" />
        <Setter Property="FontWeight" Value="SemiBold" />
        <Setter Property="Foreground" Value="{StaticResource TextBrush}" />
    </Style>

    <Style x:Key="PageSubtitleTextStyle" TargetType="TextBlock">
        <Setter Property="FontSize" Value="13" />
        <Setter Property="Foreground" Value="{StaticResource SubtleTextBrush}" />
        <Setter Property="Margin" Value="0,10,0,0" />
    </Style>

    <Style x:Key="PageHeaderSubtitleTextStyle"
           TargetType="TextBlock"
           BasedOn="{StaticResource PageSubtitleTextStyle}">
        <Style.Triggers>
            <Trigger Property="Text" Value="">
                <Setter Property="Visibility" Value="Collapsed" />
            </Trigger>
        </Style.Triggers>
    </Style>

    <Style x:Key="PageHeaderMetaTextStyle" TargetType="TextBlock">
        <Setter Property="FontSize" Value="13" />
        <Setter Property="FontWeight" Value="SemiBold" />
        <Setter Property="Foreground" Value="{StaticResource PrimaryBrush}" />
        <Setter Property="VerticalAlignment" Value="Center" />
    </Style>

    <Style x:Key="EmptyStateTitleTextStyle" TargetType="TextBlock">
        <Setter Property="FontSize" Value="22" />
        <Setter Property="FontWeight" Value="SemiBold" />
        <Setter Property="HorizontalAlignment" Value="Center" />
        <Setter Property="VerticalAlignment" Value="Center" />
    </Style>

    <Style x:Key="PageSummaryCardStyle" TargetType="Border" BasedOn="{StaticResource CardStyle}" />

    <Style x:Key="PageSummaryContentStyle" TargetType="StackPanel">
        <Setter Property="Margin" Value="18,0" />
        <Setter Property="VerticalAlignment" Value="Center" />
    </Style>
</ResourceDictionary>
