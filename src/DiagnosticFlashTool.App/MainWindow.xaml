<Window x:Class="DiagnosticFlashTool.App.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:shell="clr-namespace:System.Windows.Shell;assembly=PresentationFramework"
        xmlns:views="clr-namespace:DiagnosticFlashTool.App.Views"
        Title="BOOT刷写工具"
        Width="1440"
        Height="860"
        MinWidth="1180"
        MinHeight="760"
        WindowStyle="None"
        ResizeMode="CanResize"
        Background="{StaticResource AppBackgroundBrush}">
    <shell:WindowChrome.WindowChrome>
        <shell:WindowChrome CaptionHeight="0"
                            CornerRadius="0"
                            GlassFrameThickness="0"
                            ResizeBorderThickness="6"
                            UseAeroCaptionButtons="False" />
    </shell:WindowChrome.WindowChrome>


    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="*" />
            <RowDefinition Height="36" />
        </Grid.RowDefinitions>

        <TabControl Grid.Row="0"
                    x:Name="MainTabs"
                    Style="{StaticResource ShellTabControlStyle}"
                    ItemContainerStyle="{StaticResource ShellTabItemStyle}"
                    SelectedIndex="{Binding SelectedShellIndex, Mode=TwoWay}">
            <TabItem Header="固件刷写" Tag="⚡">
                <views:FirmwareFlashView />
            </TabItem>

            <TabItem Header="功能检测" Tag="⌕">
                <views:FunctionCheckView />
            </TabItem>

            <TabItem Header="配方配置" Tag="▤">
                <views:RecipeConfigView />
            </TabItem>

            <TabItem Header="开发者选项" Tag=">_">
                <views:DeveloperOptionsView />
            </TabItem>

            <TabItem Header="流程配置" Tag="⌘">
                <views:FlowConfigView />
            </TabItem>

            <TabItem Header="项目配置" Tag="□" Height="54" Margin="8,2,7,0" VerticalAlignment="Top">
                <views:ProjectConfigView />
            </TabItem>

            <TabItem Header="系统设置" Tag="⚙">
                <views:SystemSettingsView />
            </TabItem>
        </TabControl>

        <Border Grid.Row="1"
                Background="{StaticResource PanelBrush}"
                BorderBrush="{StaticResource CardBorderBrush}"
                BorderThickness="1,1,0,0">
            <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                <TextBlock Text="实时日志"
                           Margin="14,0,44,0"
                           FontSize="14"
                           Foreground="{StaticResource MutedTextBrush}" />
                <TextBlock Text="{Binding StatusBarContextText}"
                           FontSize="14"
                           FontWeight="SemiBold"
                           Foreground="{StaticResource TextBrush}" />
            </StackPanel>
        </Border>
    </Grid>
</Window>
